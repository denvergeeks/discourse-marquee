{{#if (theme-setting "marquee_location_below_header_bar")}}
    {{#if (theme-setting "marquee_enabled")}}
        {{#if (theme-setting 'marquee_only_visible_to_logged_in_users')}}
            {{#if (and this.currentUser (theme-setting "marquee_enabled"))}}
                <article id="marquee-bar">
                    Loading…
                </article>
            {{/if}}
        {{else}}
            <article id="marquee-bar">
                Loading…
            </article>
        {{/if}}
    {{/if}}
{{/if}}
